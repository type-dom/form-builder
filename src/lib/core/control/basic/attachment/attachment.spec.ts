import { AttachmentControl } from './attachment.class';

describe('AppElement', () => {
  let control: AttachmentControl;

  beforeEach(() => {
    // control = new AttachmentControl();
  });

  it('should create successfully', () => {
    // expect(control).toBeTruthy();
  });

  it('should have a greeting', () => {
    // control.connectedCallback();
    //
    // expect(control.querySelector('h1').innerHTML).toContain(
    //   'Welcome form-designer'
    // );
  });
});
